%h3.pull-right
  %small.text-uppercase Joined
  {{cust.created_at | date}}
%h2.h3
  {{cust.first_name | name}} {{cust.last_name | name}}
  %small {{cust.username}}
.pull-right{ 'ng-if': 'viewDetailsFunction' }
  %button{ class: 'btn btn-small btn-primary',
    'ng-click': 'viewDetailsFunction(cust)' } View Details...
%h4 {{cust.email}}
